package mdcbot.listeners;

import mdcbot.ILoggable;
import net.dv8tion.jda.core.hooks.ListenerAdapter;

public class ListenerBase extends ListenerAdapter implements ILoggable
{
    //Don't need anything here. Just want a base class which implements ILoggable
}
